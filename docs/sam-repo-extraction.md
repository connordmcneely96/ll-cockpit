# Sam's Repo Extraction — leadership-legacy (COMPLETE)\n\n> Full value extraction from `SamPrimeaux/leadership-legacy`.\n> Stack: Vite + vanilla React + Cloudflare Worker.\n> Updated: May 7, 2026 — includes full docs/, sql/d1/, sql/supabase/ review including 37KB CMS runtime.\n> Nothing is copied directly — all patterns are adapted for Next.js 15 + TypeScript + Tailwind.\n\n---\n\n## 0. WHAT WAS MISSED FIRST PASS — THE CMS\n\nSam built a complete full-stack CMS runtime in `sql/d1/010_full_cms_runtime.sql` (37KB).\nThis is the backbone of the Leadership Legacy public site AND the foundation of the NEXUS PaaS.\nIt covers everything from tenant management to page versioning to lead CRM to SEO to publishing jobs.\n\n---\n\n## 1. THE FULL CMS D1 SCHEMA (sql/d1/010_full_cms_runtime.sql — 37KB)\n\nThis is the most valuable file in Sam's repo. 13 groups, 30+ tables, 3 views, full seed data.\n\n### Group 00 — System / Schema Metadata\n```sql\ncms_schema_migrations (id, name, applied_at, checksum)         — migration audit trail\ncms_runtime_settings (id, setting_key, setting_value, is_public) — runtime config store\n```\n\n### Group 01 — Tenancy / Workspace / Users (PaaS Foundation)\n```sql\ncms_tenants (id, slug, name, status, metadata_json)             — one row per client\ncms_workspaces (id, tenant_id, slug, name, default_locale, timezone) — one per project/brand\ncms_users (id, tenant_id, email, display_name, role, status)    — role: owner/admin/editor/viewer\n```\nForeign keys cascade on delete. Unique constraint: `(tenant_id, slug)` per workspace. `(tenant_id, email)` per user.\n\n### Group 02 — Brand / Theme / Design Tokens\n```sql\ncms_brand_settings (id, tenant_id, workspace_id, brand_name, founder_name, company_name,\n  tagline, logo_url, mark_url, wordmark_url, favicon_url, og_default_image_url,\n  tokens_json, typography_json, social_links_json, contact_json)\n\ncms_themes (id, tenant_id, workspace_id, name, slug, mode, tokens_json, css_vars_json,\n  brand_json, compiled_css, compiled_css_hash, r2_key, public_url)\n```\n`tokens_json` stores the full design token map. `css_vars_json` stores compiled CSS vars.\nSeed: Connor's actual brand tokens:\n```json\n{\n  \"background\": \"#070b12\",\n  \"backgroundSoft\": \"#0d1320\",\n  \"surface\": \"#111827\",\n  \"text\": \"#f5f7fb\",\n  \"textMuted\": \"#9ca8bd\",\n  \"primary\": \"#38bdf8\",\n  \"primaryStrong\": \"#0ea5e9\",\n  \"accent\": \"#22c55e\",\n  \"accentWarm\": \"#f59e0b\",\n  \"border\": \"rgba(148, 163, 184, 0.18)\",\n  \"glass\": \"rgba(15, 23, 42, 0.72)\"\n}\n```\nTypography: `Satoshi, Inter, system-ui` (display) + `Inter` (body) + `JetBrains Mono` (mono)\n\n### Group 03 — Navigation / Menus\n```sql\ncms_navigation_menus (id, tenant_id, workspace_id, menu_key, name, location, items_json)\n```\n`location` enum: primary, footer, sidebar, etc.\nSeed: Connor's 5 primary nav items (Services, Work, About, Resources, Contact) as `items_json` array.\n\n### Group 04 — Pages / Sections / Components (Full Page Builder)\n```sql\ncms_templates (id, workspace_id, template_key, template_type, schema_json, default_sections_json)\n  — template_type: page/post/service/case_study/resource/legal/system\n\ncms_pages (id, workspace_id, title, slug, route_path, page_type, status, visibility,\n  template_id, theme_id, is_homepage, is_system_page,\n  seo_json, open_graph_json, draft_json, published_json,\n  r2_draft_key, r2_published_key, public_url,\n  created_by, updated_by, published_by, published_at)\n  — status: draft/review/published/archived\n  — UNIQUE(workspace_id, route_path)\n\ncms_page_sections (id, page_id, section_type, section_key, order_index, status,\n  props_json, style_json, responsive_json, animation_json)\n  — UNIQUE(page_id, section_key)\n\ncms_section_components (id, section_id, component_type, component_key, order_index,\n  props_json, style_json)\n\ncms_page_versions (id, page_id, version_number, version_label, snapshot_json,\n  snapshot_hash, r2_snapshot_key, created_by)\n  — Full page version history. UNIQUE(page_id, version_number).\n```\n\nViews:\n- `v_cms_page_status` — page count by workspace + status\n- `v_cms_recent_activity` — last 100 activity log entries\n- `v_cms_lead_pipeline` — lead count by workspace + status\n\n### Group 05 — Media / R2 Asset Registry\n```sql\ncms_r2_buckets (id, workspace_id, binding_name, bucket_name, public_dev_url,\n  custom_domain_url, s3_endpoint, catalog_uri, status)\n\ncms_assets (id, workspace_id, bucket_id, asset_key, object_key, url, filename,\n  mime_type, asset_type, size_bytes, width, height, duration_seconds,\n  alt_text, caption, usage_context, tags_json, variants_json, status)\n  — asset_type: image/svg/video/model/pdf/download/texture/audio/json/code/other\n  — status: uploading/ready/processing/needs_optimization/archived/failed\n  — UNIQUE(workspace_id, asset_key)\n```\n\n### Group 06 — Business Content: Services, Case Studies, Resources\n```sql\ncms_services (id, workspace_id, page_id, slug, title, eyebrow, summary, body_json,\n  starting_at, pricing_json, deliverables_json, use_cases_json, stack_json, cta_json,\n  seo_json, status, order_index)\n\ncms_case_studies (id, workspace_id, page_id, slug, title, category, client_name,\n  summary, problem, solution, outcome, body_json, stack_json,\n  metrics_json, gallery_json, testimonial_json, seo_json, status, featured, order_index)\n\ncms_resources (id, workspace_id, page_id, slug, title, resource_type, summary,\n  body_json, download_asset_id, seo_json, status)\n  — resource_type: article/checklist/playbook/download/guide/tool\n```\n\n### Group 07 — Forms / Leads / CRM\n```sql\ncms_forms (id, workspace_id, form_key, name, fields_json, success_message,\n  notification_json, status)\n\ncms_form_submissions (id, workspace_id, form_id, form_key, source_page,\n  payload_json, ip_hash, spam_score, status)\n  — status: new/reviewed/spam/archived\n\ncms_leads (id, workspace_id, submission_id, name, email, company, phone,\n  project_type, budget_range, timeline, message, source_page,\n  status, priority, owner_user_id, notes, metadata_json)\n  — status: new/qualified/proposal/won/lost/spam/archived\n  — priority: low/normal/high/urgent\n```\nThis IS the SCOUT lead CRM. Every Apollo lead → cms_leads. Every form submit → cms_form_submissions → cms_leads.\n\n### Group 08 — SEO / Redirects\n```sql\ncms_redirects (id, workspace_id, from_path, to_path, status_code, is_active, hit_count)\n  — status_code: 301/302/307/308. Unique(workspace_id, from_path).\n\ncms_seo_audits (id, workspace_id, page_id, route_path, score, issues_json, recommendations_json)\n  — score 0-100. SENTINEL can populate this automatically.\n```\n\n### Group 09 — Publishing Jobs / Activity Log\n```sql\ncms_publish_jobs (id, workspace_id, job_type, status, target_id, requested_by,\n  started_at, completed_at, error_message, result_json)\n  — job_type: page/site/assets/theme/navigation\n  — status: queued/running/success/failed/cancelled\n\ncms_activity_log (id, workspace_id, actor_user_id, action, entity_type, entity_id,\n  route_path, before_json, after_json, metadata_json)\n  — Full audit trail of every CMS action.\n```\n\n### Group 10 — Lightweight Analytics\n```sql\ncms_analytics_events (id, workspace_id, event_name, page_path, visitor_id,\n  session_id, lead_id, referrer, utm_json, metadata_json)\n```\nKeep lightweight in D1. Heavy analytics go to Supabase (ll_site_events).\n\n### Group 11 — AI Provider / Model Routing (D1 Side)\n```sql\ncms_ai_providers (id, workspace_id, provider_key, display_name, secret_name, status, use_cases_json)\ncms_ai_models (id, workspace_id, provider_key, model_key, display_name, lane, runtime,\n  modality, is_enabled, is_blocked, input_price_per_mtok, output_price_per_mtok, cached_input_price_per_mtok)\n  — modality: text/image/audio/embedding/multimodal\ncms_ai_routing_policy (id, workspace_id, policy_key, default_text_model, cheap_text_model,\n  senior_text_model, review_provider, router_strategy, blocked_models_json)\ncms_ai_routing_arms (id, workspace_id, provider_key, model_key, task_type, mode,\n  alpha, beta, total_runs, successes, failures, avg_latency_ms, avg_cost_usd, avg_quality_score)\n  — This is the D1-side Thompson Sampling config (mirrors Supabase ll_routing_arms)\n```\n\n**Key insight:** Sam stores the routing arms in BOTH D1 (for fast Worker reads) AND Supabase (for heavy analytics). D1 drives the live routing decision. Supabase stores the telemetry and historical arm performance.\n\n### Seed Data Included\n- Tenant: `tenant_leadership_legacy`\n- Workspace: `ws_leadership_legacy`\n- User: Sam as owner\n- Brand: Connor's full token set (background, text, primary, accent, border, glass)\n- Theme: Leadership Legacy Dark\n- R2 bucket: leadership-legacy\n- Navigation: 5 primary nav items\n- Homepage page: published, route_path=/\n- AI providers: Anthropic + OpenAI with model lanes\n- Routing policy: default (claude-sonnet=default, gpt-5.4-nano=cheap, claude-sonnet=review)\n\n---\n\n## 2. D1 Schema for ll-cockpit (New Migrations)\n\n### Migration 0004 — AI Provider Registry\n```sql\ncms_ai_providers, cms_ai_models, cms_ai_routing_policy\n```\nSeed: Anthropic (sonnet-4-5=default_workhorse, haiku=cheap_fast), blocked models policy.\n\n### Migration 0005 — R2 Asset Registry\n```sql\ncms_r2_buckets, cms_r2_objects (simple catalog), cms_assets (full media library)\n```\n\n### Migration 0006 — Analytics\n```sql\nanalytics_events, analytics_sessions, ai_completions, ai_routing_decisions\n```\n\n### Migration 0007 — Agent Tables\n```sql\ncontent_queue, subscribers, revenue, pending_approvals\n```\n\n### Migration 0008 — Research Pipeline\n```sql\nresearch_sources, research_queue, research_digests, research_project_links\n```\n\n### Migration 0009 — Full CMS Runtime (from Sam's 010_full_cms_runtime.sql)\n```sql\ncms_schema_migrations, cms_runtime_settings,\ncms_tenants, cms_workspaces, cms_users,\ncms_brand_settings, cms_themes,\ncms_navigation_menus,\ncms_templates, cms_pages, cms_page_sections, cms_section_components, cms_page_versions,\ncms_assets,\ncms_services, cms_case_studies, cms_resources,\ncms_forms, cms_form_submissions, cms_leads,\ncms_redirects, cms_seo_audits,\ncms_publish_jobs, cms_activity_log,\ncms_analytics_events,\ncms_ai_routing_arms\n```\nApply file: `sql/d1/010_full_cms_runtime.sql` from Sam's repo.\nThis migration is how Cockpit manages the public site `leadershiplegacydigital.com`.\n\n### Migration 0010 — CMS Triggers (from Sam's 011)\nD1 triggers for updated_at timestamps on CMS tables.\n\n### Migration 0011 — CMS Seed Content (from Sam's 012)\nSeed pages, services, case studies for Connor's public site.\n\n---\n\n## 3. Supabase Schema — Full PaaS Telemetry Layer\n\n24 tables with RLS enabled. See section above in previous read for full details.\nKey additions now understood:\n\n- `ll_tenants` + `ll_workspaces` = Supabase mirror of D1 tenant structure\n- `ll_routing_arms` = Supabase-side Thompson Sampling (heavy analytics side)\n- D1 `cms_ai_routing_arms` = same concept, D1-side for fast live routing\n- Together: live routing decisions in D1, telemetry/history in Supabase\n\n---\n\n## 4. Worker API Routes\n\n| Route | Description | Priority |\n|---|---|---|\n| `GET /api/health` | All binding presence checks | P1 |\n| `GET /api/ai/providers` | Live AI provider secret status | P1 |\n| `GET /api/r2/list?prefix=` | List R2 objects | P1 |\n| `GET /api/r2/text?key=` | Read text file from R2 | P1 |\n| `GET /api/r2/object/[...key]` | Stream raw R2 object | P2 |\n| `GET /api/r2/status` | R2 bucket status | P2 |\n| `POST /api/agent/code` | Anthropic code assistance | P1 |\n| `GET /api/github/status` | GitHub OAuth secret status | P2 |\n| `GET /api/oauth/github/start` | GitHub OAuth redirect | P3 |\n| `GET /api/oauth/google/start` | Google OAuth redirect | P3 |\n| `POST /api/cms/pages` | Create/update CMS page | P3 |\n| `GET /api/cms/pages/:id` | Read CMS page | P3 |\n| `POST /api/cms/publish` | Trigger publish job | P3 |\n| `POST /api/leads` | Create lead from form submission | P2 |\n| `POST /api/forms/:key/submit` | Submit a form → lead | P2 |\n| `POST /api/cad/jobs` | Trigger OpenSCAD job | P4 |\n\n---\n\n## 5. RUBRIC.md — PaaS Readiness Scoring\n\n7 categories, score 0-5, target 4 on all before production:\nIntegration Setup, Secret Safety, Dashboard Usability, AI Tooling, Tool Execution Safety, Testing, Connor Readiness.\n\n---\n\n## 6. KV Namespace Strategy (5 Namespaces)\n\n```\nLL_SESSIONS, LL_RATE_LIMITS, LL_CACHE, LL_OAUTH_STATE, LL_FLAGS\n```\n\n---\n\n## 7. Durable Objects Plan\n\n```\nDashboardSession — live CMS editing\nAgentSession     — agent run + PTY terminal state\nCMSCollaborationSession — multi-user editing\n```\n\n---\n\n## 8. Workspace Registration Pattern (PaaS Core)\n\n```sql\nagentsam_workspace (id, workspace_slug, name, r2_bucket, r2_prefix, github_repo, default_model_id)\nagentsam_scripts (id, workspace_id, name, path, purpose, runner, safe_to_run, owner_only)\n```\n\n---\n\n## 9. End-to-End Integration Playbook (10 Phases)\n\nPhase 1: Foundation → Phase 2: CF Resources → Phase 3: Supabase → Phase 4: AI → Phase 5: Email → Phase 6: Google → Phase 7: GitHub → Phase 8: Tool Registry → Phase 9: Monitoring → Phase 10: Handoff\n\n---\n\n## 10. Thompson Sampling AI Routing\n\nRouting arms in D1 (`cms_ai_routing_arms`) for live decisions.\nRouting arms in Supabase (`ll_routing_arms`) for analytics + optimization.\nalpha++ on success, beta++ on failure. Highest Beta(alpha, beta) sample wins.\n\n---\n\n## 11. Integration Surface (Full PaaS)\n\nAnthropic, OpenAI, Gemini, Workers AI, Resend, Gmail, Google Drive, GitHub, Supabase, D1, R2, KV, DO, Spline, OpenSCAD, Local Llama/Ollama.\n\n---\n\n## 12. CAD + OpenSCAD\n\nServer-side tool. STL → R2 → download. Routes: `/api/cad/jobs`, `/api/cad/jobs/:id`, `/api/cad/jobs/:id/download`.\n\n---\n\n## 13. UI Patterns\n\n`useResizablePanels` hook, Monaco file tree from R2, terminal preset strip, agent-to-editor apply.\n\n---\n\n## 14. Dashboard Pages to Add\n\n| Page | Route | Priority |\n|---|---|---|\n| AI Providers | `/ai-providers` | P1 |\n| R2 Storage | `/storage` | P1 |\n| Analytics | `/analytics` | P2 |\n| Leads | `/leads` | P2 |\n| CMS Pages | `/cms` | P2 — manages public site |\n| Media Library | `/media` | P3 |\n| Services | `/services` — manages cms_services | P3 |\n| Case Studies | `/case-studies` — manages cms_case_studies | P3 |\n\n---\n\n## 15. Safety Rules\n\n```\n❌ Never commit secrets\n❌ Never expose Supabase service-role keys to the browser\n❌ Never store OAuth refresh tokens in localStorage\n❌ Never run untrusted OpenSCAD without sandboxing\n❌ Never route expensive models without cost logging\n❌ Never image-gen without quotas\n❌ Never use client-side-only password in production\n```\n\n---\n\n## Connor's Brand Tokens (from seed data — canonical)\n\n```\nbackground:     #070b12\nbackgroundSoft: #0d1320\nsurface:        #111827\nsurfaceElevated: #172033\ntext:           #f5f7fb\ntextMuted:      #9ca8bd\nprimary:        #38bdf8  (sky blue)\nprimaryStrong:  #0ea5e9\naccent:         #22c55e  (green)\naccentWarm:     #f59e0b  (amber)\nborder:         rgba(148, 163, 184, 0.18)\nglass:          rgba(15, 23, 42, 0.72)\n\ndisplay font:   Satoshi, Inter, system-ui\nbody font:      Inter, system-ui\nmono font:      JetBrains Mono, SFMono-Regular, monospace\n```\n\nThese are Connor's public site brand tokens, separate from the Cockpit blue-black palette.\n\n---\n\n## Build Sequencing (Updated)\n\n1. Resizable panels\n2. R2 API routes\n3. Wire Monaco IDE\n4. D1 migrations 0004-0008\n5. AI Providers page\n6. R2 Storage page\n7. Analytics page\n8. **D1 migration 0009 — Full CMS runtime** (apply Sam's 010_full_cms_runtime.sql to ll-cockpit-db)\n9. **CMS pages in Cockpit** (/cms, /leads, /services, /case-studies)\n10. Supabase telemetry tables\n11. Thompson Sampling routing\n12. OAuth flows\n13. Workspace registration pattern (Sprint 10)\n14. Rubric readiness gate (Sprint 10)\n